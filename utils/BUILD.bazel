load("@buildifier_prebuilt//:rules.bzl", "buildifier")

buildifier(
    name = "buildifier_check",
    exclude_patterns = [
        "./.git/*",
    ],
    lint_mode = "warn",
    mode = "diff",
)

buildifier(
    name = "buildifier_fix",
)
